# 홈페이지 빌더 (FRONTIER SITE BUILDER)

프론티어 M&A 아카데미의 디자인 체계를 부품으로 쪼갠 **위지윅 홈페이지 제작 도구**입니다.
구역(Section)을 쌓아 사이트를 만들고, **HTML 파일 한 장으로 내보내** 어디에나 올릴 수 있습니다.

- **주소**: https://mnaedu.vercel.app/builder
- 검색에는 노출되지 않습니다(`robots: noindex`).

---

## 0. 사용법은 빌더 안에 있습니다

이 문서와 **같은 내용을 빌더 화면 안에서 바로** 보실 수 있습니다.

- **오른쪽 위 「? 사용법」** — 전체 매뉴얼 8장 (차례를 눌러 이동, 인쇄 가능)
- **메뉴 옆의 작은 「?」** — 그 메뉴만 설명하는 쪽지 (Esc 또는 바깥 클릭으로 닫힘)

「?」는 13곳에 있습니다: 사이트 이름 · 되돌리기 · 기기 전환 · 미리보기 ·
저장/내보내기 · 구역 탭 · 구역 단추 · 추가 탭 · 테마 탭 · 글자 고치기 ·
배치 · 배경 · 내용.

> 안내 문장은 `lib/builder/help.ts` 한곳에 모여 있습니다. 메뉴를 늘리면
> 여기에 안내를 함께 넣어야 하며, 빠뜨리면 테스트가 잡아냅니다.

---

## 1. 화면 구성

| 자리 | 하는 일 |
| --- | --- |
| 왼쪽 「구역」 | 구역 목록 — 선택·순서 변경(↑↓)·숨기기(●)·복제(⧉)·삭제(✕) |
| 왼쪽 「추가」 | 11가지 구역을 골라 넣기 (선택한 구역 바로 아래에 들어갑니다) |
| 왼쪽 「테마」 | 색 5종 견본, 색상 8개 직접 지정, 제목 글꼴, 모서리 둥글기, 글자 크기 |
| 가운데 캔버스 | 실제 화면. **글자를 눌러 그 자리에서 고칩니다** |
| 오른쪽 속성판 | 고른 구역의 배경·여백·폭·정렬·글자색과 내용 항목 |
| 위쪽 막대 | 사이트 이름, 되돌리기/다시실행, 기기 전환, 미리보기, 내보내기, 「? 사용법」 |

---

## 2. 글자 고치기

캔버스에서 **글자를 직접 누르면 커서가 생깁니다.** 고친 뒤

- `Enter` — 확정
- `Esc` — 되돌리기
- 다른 곳 클릭 — 확정

같은 내용을 오른쪽 속성판에서도 고칠 수 있습니다. 둘은 같은 값을 봅니다.

---

## 3. 구역 11종

| 구역 | 쓰임 |
| --- | --- |
| 상단 메뉴 | 로고·메뉴·우측 버튼 |
| 히어로 | 큰 제목, 설명, 버튼 2개, 성과 숫자 |
| 카드 그리드 | 분야·서비스·상품 (2·3·4열, 카드마다 사진) |
| 단계 흐름 | STEP 01~04 진행 절차 (중요 단계 강조 가능) |
| 비교표 | 등급·요금 비교 (칸에 `O`/`X`를 넣으면 강조) |
| 목록 | 번호가 붙는 줄 목록 + 꼬리표 |
| 이미지 갤러리 | 사진 격자 (2·3·4열) |
| 글 구역 | 제목 + 여러 문단 본문 |
| 강조 배너 | 문의·신청을 부르는 띠 |
| **게시판** | 공지·자료실 (아래 5절) |
| 푸터 | 회사 정보와 하단 링크 |

---

## 4. 배경·이미지

구역마다 배경을 따로 정합니다.

- **기본 / 단색 / 그러데이션(두 색 + 각도) / 사진**
- 사진 배경은 **어둡게 덮기**를 조절해 글자가 묻히지 않게 합니다.
- 사진은 **주소를 붙여 넣거나 파일을 올립니다.** 올린 사진은 가로 1600px로 줄여
  브라우저에 저장되므로, 사진이 아주 많으면 저장 공간이 찰 수 있습니다
  (그때는 위쪽 막대의 **저장(JSON)** 으로 파일로 빼 두십시오).

---

## 5. 게시판

글 목록 → 읽기 → 글쓰기가 실제로 동작합니다.

- 빌더에서 넣은 글은 **내보낸 HTML 파일에 함께 담겨** 나갑니다.
- 방문자가 새로 쓴 글은 **그 사람의 브라우저에만** 남습니다(서버가 없으므로).
- 편집 화면에서는 목록 모양만 보이고, **「미리보기」에서 실제로 동작**합니다.

> 여러 사람이 함께 보는 진짜 게시판이 필요하면 서버(DB) 연결이 따로 필요합니다.
> 지금 구조는 소개용 홈페이지의 공지 게시판을 상정한 것입니다.

---

## 6. 저장과 내보내기

| 단추 | 하는 일 |
| --- | --- |
| (자동) | 고칠 때마다 브라우저에 자동 저장됩니다. 다시 열면 이어서 작업합니다 |
| **HTML 내보내기** | 사이트를 **파일 한 장**으로. 이 파일만 있으면 어디에 올려도 그대로 뜹니다 |
| **저장(JSON)** | 작업 문서를 파일로 보관 — 다른 컴퓨터로 옮기거나 백업할 때 |
| **불러오기** | 저장해 둔 JSON을 다시 열기 |
| **처음부터** | 시작 템플릿으로 되돌리기 |

내보낸 HTML은 스타일이 파일 안에 들어 있어 **인터넷 없이도** 열립니다
(웹폰트만 인터넷에서 받아오며, 없으면 시스템 글꼴로 대체됩니다).

---

## 7. 만든 사람을 위한 메모 (구조)

| 파일 | 역할 |
| --- | --- |
| `lib/builder/types.ts` | 문서 모델 · 구역 정의 · 테마 견본 (순수 데이터) |
| `lib/builder/render.ts` | **마크업과 스타일** — 편집 화면과 내보내기가 함께 쓴다 |
| `lib/builder/export.ts` | 자립형 HTML 생성 + 게시판 동작 스크립트 |
| `lib/builder/template.ts` | 시작 템플릿 (이 사이트의 골격) |
| `lib/builder/help.ts` | **사용법 문장** — 메뉴별 쪽지와 전체 매뉴얼 |
| `app/builder/builder-client.tsx` | 편집기 본체 |
| `app/builder/builder.css` | 작업대 UI 스타일 (`bx-` 접두사) |
| `tests/builder.test.mjs` | 회귀 테스트 14건 |

설계에서 지켜야 할 두 가지:

1. **편집 화면과 내보내기는 반드시 같은 함수(`renderSection`)에서 나온다.**
   둘을 따로 만들면 "편집 중에 본 모습"과 "내보낸 결과"가 어긋나는데, 빌더에서
   가장 치명적인 고장이다.
2. **편집 표시(`contenteditable`)는 마크업에 담는다.** 그려진 뒤에 자바스크립트로
   붙이면 화면을 다시 그릴 때마다 지워진다 — 그린 결과의 주인은 리액트다.
   같은 이유로 입력 이벤트도 글자마다 달지 않고 캔버스 한 곳에서 받는다.

사용자가 넣은 모든 글자는 `escapeHtml`을, 이미지 주소는 `safeUrl`을 거친다
(`http(s)`·`data:image`·같은 서버 경로만 통과). 내보낸 파일이 스크립트를
실어 나르지 않게 하는 방어선이다.

세 번째로, **안내는 화면 코드에 흩어 두지 않는다.** 메뉴가 바뀔 때 안내만
옛말로 남기 때문이다. `help.ts`에 모으고 화면은 id로 불러다 쓰며, 어느
메뉴에 안내가 빠졌는지는 `HELP_IDS`와 테스트가 알려 준다.
