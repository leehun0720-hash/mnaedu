# 프론티어 M&A 아카데미 · 운영 매뉴얼

이 문서는 지금까지 구축한 홈페이지와 아카데미의 **전 기능과 운영 방법**을 한곳에
정리한 것입니다. 회장님께 드리는 요약 안내는 별도 문서(`기능안내서.md`)에 있고,
이 매뉴얼은 실제로 운영하며 참고하는 상세본입니다.

- **배포 주소**: https://mnaedu.vercel.app
- **관리자 화면**: https://mnaedu.vercel.app/admin
- 관련 문서: 설정은 `SUPABASE.md`, 출제 관리자 설정은 `ADMIN.md`, 결제 연동은
  `BILLING.md`, 코드 구조는 `README.md`.

---

## 1. 전체 구조

사이트는 첫 화면(게이트웨이)에서 두 갈래로 나뉩니다.

| 경로 | 설명 |
| --- | --- |
| `/` | 게이트웨이 — 로고 애니메이션, [기업 홈페이지] · [퀴즈 아카데미] 진입 |
| `/company` | 기업 홈페이지 — 새 소식, 회사소개, 주요업무(M&A 오피스 3분야 · 시크릿 오피스 2분야), 직원채용, Q&A, 문의 |
| `/company/business/<분야>` | 업무 상세 — 분야별 소개·커리큘럼 (소속 오피스 배지 표시) |
| `/academy` | 퀴즈 아카데미 — 문제은행, 수강 여정, 회원 등급, 5레벨, 선발 테스트 |
| `/academy/quiz/<번호>` | 문제 풀이 — 답안 제출 → 채점 → 점수·포인트 → 해설 |
| `/academy/me` | 내 학습 현황 — 등급·포인트·점수 집계·레벨 열림 상태 |
| `/academy/billing` | 유료회원 전환 — 이용권 신청·이용 기간·신청 내역 |
| `/academy/help` | 이용 안내 (회원용 도움말) |
| `/academy/join` · `/academy/login` | 회원가입 · 로그인 |
| `/privacy` | 개인정보처리방침 |
| `/admin` | 출제·회원·결제 관리 (회장 전용) |
| `/builder` | 홈페이지 빌더 — 구역을 쌓아 사이트를 만들고 HTML로 내보내는 도구 (`BUILDER.md`) |

양쪽 화면 오른쪽 고정 바로 두 사이트를 오갑니다. **회원가입 창구는 아카데미
하나**이며, 홈페이지에는 가입 메뉴가 없습니다.

---

## 2. 회원 등급과 권한

무료·유료 2등급입니다.

| 구분 | 열리는 범위 |
| --- | --- |
| 비회원 | 업무 소개·커리큘럼 목차 열람 |
| 무료회원 | 위 + L1 입문 문제 풀이 + 포인트 적립 |
| 유료회원 | 위 + L2~L5 전문가 문제 + 회장 해설 열람 |
| L5 통과 | 위 + 오프라인 초전문가 과정 초대 심사 대상 |

- **유료 자격은 기간제입니다.** 이용 기간(`이용권`으로 부여)이 지나면 자동으로
  무료 범위로 돌아갑니다. 관리자가 직접 올린 계정(회장·심사용)은 기한이 없습니다.
- 등급이 만료되어도 **적립 포인트와 통과 레벨은 유지**됩니다.

---

## 3. 문제 풀이 → 채점 → 점수 → 포인트

회원이 겪는 핵심 흐름입니다.

1. **풀이** — 선발 테스트에서 문제 카드를 눌러 풀이 화면으로 들어갑니다.
   주관식은 30자 이상 서술, 객관식은 보기 선택. 한 문제는 한 번만 제출됩니다.
2. **채점**
   - 객관식: 제출 즉시 자동 채점(100/0점).
   - 주관식: 관리자 화면 **채점함**에서 회장이 0~100점과 강평을 매깁니다.
     (`ANTHROPIC_API_KEY` 설정 시 L1~L3는 AI가 즉시 채점 — 4절 참조)
3. **점수 → 포인트** — 60점 이상이면 통과. 통과 시 퀴즈 포인트 +10P가 적립되고
   통과 레벨이 갱신됩니다.
4. **해설** — 정답 표시 아래 해설 영역. 무료회원은 흐리게 가려져 보이고,
   유료회원이 열람 버튼을 눌러 30P를 차감하면 전문이 열립니다. 한 번 연 해설은
   다시 차감되지 않습니다.

**포인트 기준값**: 가입 축하 60P · 문제 통과 10P · 해설 열람 −30P. 이 값들은
`lib/membership.ts`의 `POINTS` 한 곳에서 정의되며, 도움말과 화면이 모두 그 값을
따릅니다.

---

## 4. 관리자 화면 (`/admin`)

비밀번호로 로그인합니다. 화면은 위에서 아래로 여섯 영역입니다.

### (1) 붙여넣기로 초안 만들기
문제를 통째로 붙여넣으면 분야·난이도·유형·보기·정답을 추정해 아래 입력란을
채웁니다. 추정이므로 저장 전에 반드시 확인하십시오.

### (2) 새 문제 / 문제 수정
분야·난이도·유형·문제 본문·정답·출제 의도·**해설**을 입력합니다.
- **정답·출제 의도**는 회원에게 공개되지 않습니다.
- **해설**은 유료회원이 포인트로 여는 회장 해설입니다.
- ‘발행’을 켠 문제만 선발 테스트에 최신순으로 노출됩니다.

### (3) 회원·포인트 관리
- 등급 전환(유료/무료)을 버튼 한 번으로. SQL이 필요 없습니다.
- 포인트 조정: `+100P` · `−100P` · `정확히 지정`(목표 잔액 + 사유 입력).
- **포인트 내역**: 가입·통과·열람·관리자 조정이 시간순으로 기록됩니다.
  회원이 잔액을 문의하면 이 화면을 그대로 근거로 보여 줄 수 있습니다.
- 이름·이메일 검색과 등급 필터로 회원이 늘어도 찾을 수 있습니다.

### (4) 결제 관리
유료 전환 신청을 확인·승인합니다. **승인하면 그 자리에서 이용 기간이 열립니다.**
남은 기간이 있으면 그 끝에 이어 붙습니다. 상태별 탭(확인 대기 / 결제 완료 /
취소 / 전체)으로 나눠 봅니다. (결제 대행 연결 전까지 유일한 승인 경로입니다.)

### (5) 채점함
회원이 낸 주관식 답안에 점수·강평을 매깁니다. 기본은 **채점 대기**만 보여
주므로 할 일이 지난 채점에 묻히지 않고, 대기 건수는 제목에 항상 떠 있습니다.

### (6) 출제 현황 (분야 × 레벨)
5분야 × 5레벨 = 25칸 격자에 분야별·레벨별 문제 수(발행 수)가 뜹니다. 빗금 친
빈 칸이 아직 출제되지 않은 자리입니다 — 목록을 훑는 대신 여기서 다음 출제를
정합니다. 칸을 누르면 그 칸의 문제만 아래 목록에 걸립니다.

### (7) 등록된 문제
20건씩 페이지. 본문 검색, 분야·레벨·상태 필터. 상태의 `미완성`은 정답이나
해설이 빈 문제만 골라내며, 목록에서 바로 발행/발행 취소가 됩니다.

> **AI 자동 채점 (선택)**: Vercel 환경변수에 `ANTHROPIC_API_KEY`를 넣으면
> 주관식도 제출 즉시 AI가 채점합니다. 단 회장 검수 영역인 L4·L5는 AI가 채점하지
> 않고 채점함으로 보냅니다. 넣지 않으면 모든 주관식이 채점함으로 갑니다.

---

## 5. 유료 결제

- 회원이 `/academy/billing`에서 이용권을 신청하면 주문이 접수(확인 대기)됩니다.
- 회장이 `/admin` 결제 관리에서 승인하면 이용 기간이 열립니다.
- **요금은 회장 승인 전까지 화면에 노출되지 않습니다.** 확정 시
  `lib/billing.ts`의 `SHOW_PRICE`를 켜면 금액이 표시됩니다. 요금제(이용권 종류·
  금액·기간)도 같은 파일에서 관리합니다.
- 토스페이먼츠 연결 절차와 금액 위변조 방어는 `BILLING.md`에 정리돼 있습니다.
  구독 연장·이력은 이미 한곳(`activateOrder`)에 모여 있어, 결제 승인 응답이 그
  함수를 부르도록 연결하는 것으로 끝납니다.

---

## 6. 콘텐츠 보호와 보안

- **복사 방지**: 우클릭·드래그 선택·복사·전체선택·인쇄·개발자도구 단축키를
  억제합니다. 관리자 화면에는 적용하지 않습니다.
- **열람자 워터마크**: 화면 캡처는 어떤 웹 기술로도 막을 수 없으므로, 회장 해설
  위에 열람자(가린 이메일)와 시각을 옅게 깔아 유출 시 출처가 드러나게 합니다.
- **화면 테마**: 방문자가 엠버(기본)·감청·세피아 세 색 중 고를 수 있습니다
  (화면 왼쪽 아래 버튼). 색만 바꾸며 대비는 유지되고, 선택은 브라우저에
  저장됩니다. 관리자 화면에는 적용되지 않습니다.
- **정답·해설 비공개(불변 원칙)**: 정답·출제 의도·해설은 공개 데이터에서 원천
  배제됩니다. 해설은 로그인 + 유료 + 풀이 선행 + 포인트 차감을 거친 전용 경로로만
  나갑니다. 유료 문제 본문 자체도 등급으로 막혀 비로그인 열람이 차단됩니다.
- **시크릿 오피스 블라인드**: 패밀리오피스·투자가 클럽 분야는 오직 오프라인
  교육·커뮤니티로만 진행하므로 문제은행에서 블라인드됩니다. 이 분야의 문제는
  발행해 두어도 공개 화면·풀이 화면·해설 경로 어디에도 나가지 않습니다(직접
  주소로 접근해도 열리지 않음). 아카데미 화면에는 주제가 흐릿하게만 비치고
  "오프라인 전용" 안내가 뜹니다. 출제해 둔 문제는 오프라인 과정에서 쓰시면 됩니다.
- **DB 접근 차단**: Supabase의 RLS와 권한 회수로 공개 키를 통한 직접 조회를
  막았습니다(7개 테이블 전부). `supabase/setup.sql`이 이를 설정합니다.
- **관리자 로그인 보호**: 반복 실패 시 잠깁니다(IP별 15분 내 10회 → 30분,
  전역 15분 내 30회 → 5분). 관리자 비밀번호는 길고 무작위한 값을 쓰십시오.
- **AI 채점 방어**: 채점 시 정답을 회원 답안과 분리하고, 돌려받은 강평이
  모범답안과 겹치면 서버가 버려 프롬프트 조작으로 정답이 새지 않게 합니다.

전체 보안 점검 이력과 조치는 저장소 커밋 기록에 남아 있습니다.

---

## 7. 아직 남은 항목 (확인·확정 대기)

- 도메인 `frontiermade.co.kr` 등록·연결
- 유료 요금 확정 후 금액 공개(`SHOW_PRICE`)와 토스페이먼츠 연결
- 문의 수신 메일 확정 후 서버 발송 연결 (현재는 메일 초안 전달 방식)
- 실적 수치 공개 여부(회장 승인 전까지 미노출)
- 회원이 늘면: 인증 메일 자체 발송(SMTP) 연결 — 현재는 Supabase 기본 발송이라
  시간당 발송 수에 제한이 있습니다.

---

## 8. 운영 중 자주 쓰는 경로

| 하고 싶은 일 | 어디서 |
| --- | --- |
| 문제 출제·발행 | `/admin` → 새 문제 |
| 어디에 문제가 비었는지 확인 | `/admin` → 출제 현황 격자 |
| 주관식 채점 | `/admin` → 채점함 |
| 회원을 유료로 전환 | `/admin` → 회원 관리 → 유료로 올리기 |
| 결제 신청 승인 | `/admin` → 결제 관리 → 승인 |
| 회원 포인트 확인·조정 | `/admin` → 회원 관리 → 포인트 내역 |
| 회원에게 이용법 안내 | `/academy/help` 링크 전달 |
