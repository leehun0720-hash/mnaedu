# ㈜프론티어 M&A 홈페이지 · 운영 매뉴얼

구축한 홈페이지의 **전 기능과 운영 방법**을 한곳에 정리한 상세본입니다.
회장님께 드리는 요약은 `기능안내서.md`에 있습니다.

- **주소**: https://www.frontierexpert.com (예비: https://mnaedu.vercel.app)
- **관리자 화면**: https://www.frontierexpert.com/admin
- 관련 문서: 설정은 `SUPABASE.md`, 관리자 설정은 `ADMIN.md`, 코드 구조는 `README.md`

---

## 0. 이 사이트의 목적

**신뢰를 증명해 상담이 들어오게 하는 것 하나입니다.**

교육 사업이 아닙니다. 온라인은 사람이 모이는 자리이고, 상담·미팅·수익은
오프라인에서 일어납니다. 그래서 이 사이트에는 다음이 **없습니다**:

- 강의 · 커리큘럼 · 수강 · 수료 · 자격증
- 레벨 · 승급 · 등급
- 채점 · 점수 · 포인트
- 유료 결제 · 구독

회장님의 손이 계속 가야 하는 기능은 두지 않는다는 것이 설계의 첫째 원칙입니다.

---

## 1. 전체 구조

한 페이지가 중심이고, 그 아래 몇 개의 화면이 붙습니다.

| 경로 | 설명 |
| --- | --- |
| `/` | **브랜드 페이지** — 회사소개, 주요업무 5분야, 실무 문제, 업무정보실, 직원채용, Q&A, 문의 |
| `/business/<분야>` | 업무 상세 — 분야별 소개·주제 (소속 오피스 배지 표시) |
| `/join` · `/login` | 회원 등록 · 로그인 (이메일) |
| `/privacy` | 개인정보처리방침 |
| `/admin` | 문제 출제 · 업무정보실 · 회원 명단 (회장 전용) |
| `/builder` | 홈페이지 빌더 — 구역을 쌓아 사이트를 만들고 HTML로 내보내는 도구 (`BUILDER.md`) |

우측 고정 바에 **실무 문제 · 업무정보실 · 회원 로그인 · 상담 신청** 네 개를 두어
어느 위치에서든 한 번에 닿습니다.

---

## 2. 실무 문제

이 사이트의 핵심 장치입니다. 흐름은 한 줄입니다 —
**잘 만든 문제를 본다 → "이 사람 대단하네" → 상담을 신청한다.**

| 대상 | 볼 수 있는 것 |
| --- | --- |
| 누구나 | 문제 본문, 객관식 보기, 분야·유형 |
| 로그인한 회원 | **정답과 해설** |

- 답안을 받지 않습니다. 제출도, 채점도, 점수도 없습니다.
- 객관식 보기는 눌러서 스스로 골라볼 수 있지만 어디에도 저장되지 않습니다.
- 발행된 문제가 하나도 없으면 예시 문제 3건이 대신 서므로 빈 화면이 나오지
  않습니다.

### 정답이 새지 않는 구조

정답·해설은 공개 페이지 데이터에 **아예 실리지 않습니다.** 브라우저 개발자
도구로 페이로드를 뒤져도 나오지 않으며, 오직 `/api/answer`가 로그인 세션을
확인한 뒤에만 내보냅니다. 이 경로가 막히면 정답이 새는 것이 아니라 열리지
않을 뿐입니다.

시크릿 오피스(패밀리오피스·투자가 클럽) 분야의 문제는 발행 여부와 무관하게
공개 목록에도 해설 경로에도 오르지 않습니다.

---

## 3. 업무정보실

회장님이 워드 문서를 올리면 그대로 목록에 섭니다.

| 항목 | 값 |
| --- | --- |
| 형식 | .doc · .docx · .pdf · .hwp · .hwpx |
| 크기 | 한 건에 8 MB |
| 열람 | **누구나** — 로그인 없이 내려받습니다 |
| 분류 | 자료 / 칼럼 · 5분야 (둘 다 선택 사항) |

파일은 별도 스토리지 버킷 없이 데이터베이스에 담습니다. 버킷·접근 정책·추가
비밀 키를 만들지 않아 설정과 관리 지점이 늘지 않습니다. 목록을 불러올 때는
파일 본문 열을 아예 선택하지 않으므로, 큰 데이터는 실제 내려받을 때만 오갑니다.

---

## 4. 회원

정답과 해설을 열기 위한 최소 장치입니다.

- 이메일 + 비밀번호 (Supabase Auth). 인증 메일의 링크를 누르면 로그인 완료.
- 등급 없음, 포인트 없음, 결제 없음. **가입은 무료입니다.**
- 관리자 화면의 **회원** 탭은 보기 전용 — 이름·이메일·등록일만 뜹니다.

회원 명단의 쓸모는 하나입니다: **관심을 보인 사람이 누구인지 아는 것.**

---

## 5. 관리자 화면 (`/admin`)

비밀번호 하나로 들어갑니다. 탭은 셋입니다.

### 문제 출제
- 붙여넣기 초안 → 분야·유형·보기·정답 자동 추정 (확인 필요)
- 정답·해설 입력 (회원 공개)
- 분야별 출제 현황 격자 — 빈 칸이 다음 출제 자리
- 검색 · 상태 필터(발행/임시/미완성) · 20건씩 페이지
- 목록에서 바로 발행/발행 취소

### 업무정보실
- 파일 업로드 (제목·설명·분야·구분)
- 발행 토글, 받아보기, 삭제

### 회원
- 명단 보기 전용

### 지원자 (직원채용)
- 채용시험을 치르고 지원 의사를 보낸 분들의 답안
- 응시자의 답 옆에 등록해 둔 정답이 나란히 — 견주며 읽고 점수를 매깁니다
- 점수 · 전형 상태(접수/검토중/합격/불합격) · 메모

상세는 `ADMIN.md`를 보십시오.

---

## 4-2. 직원채용 — 채용시험 (`/careers`)

첫 화면 **직원채용** 칸의 「채용시험 응시하기」로 들어갑니다.

한 화면에서 끝납니다 — 문제를 풀고, 그 아래에서 **「직원채용」을 골라 지원 의사를
보냅니다.** 따로 이력서를 받지 않습니다.

| 단계 | 누가 | 무엇을 |
| --- | --- | --- |
| 1 | 회장 | 관리자 → 문제 출제에서 분야를 **직원채용**으로 두고 문제를 발행 |
| 2 | 응시자 | `/careers` 에서 문제를 풀고 성함·이메일을 적고 동의 후 전송 |
| 3 | 회장 | 관리자 → 지원자에서 답안을 읽고 점수와 전형 상태를 기록 |
| 4 | 회장 | 합격 여부를 이메일로 개별 안내 |

지켜지는 것 두 가지입니다.

- **채용 문제의 정답은 응시자에게 어느 경로로도 가지 않습니다.** 응시 화면이
  받는 값에 정답 열이 아예 들어 있지 않고, 회원이 정답을 여는 길에서도 막힙니다.
- **채용 문제는 업무 화면과 첫 화면에 서지 않습니다.** 업무 문제와 섞이지 않습니다.

채점은 사람이 합니다. 서술형 답을 글자 맞춰 세는 것은 채점이 아니라 흉내이므로,
회장님이 읽고 매기십니다. 합격선 80점은 회사 소개 문구와 같은 값을 씁니다.

---

## 6. 콘텐츠 보호와 보안

- **정답·해설**: 공개 데이터에서 원천 배제. `/api/answer`만이 유일한 출구.
- **복사 방지**: 우클릭·드래그·단축키 제한과 열람자 워터마크. 완전한 차단은
  불가능하며, 목적은 분쟁 시 근거를 남기는 데 있습니다.
- **관리자 로그인**: 실패 횟수를 DB에 세어 무차별 대입을 막습니다. 서버리스에서
  메모리 카운터는 인스턴스마다 따로 놀아 소용이 없기 때문입니다.
- **비밀 관리**: 저장소가 공개이므로 어떤 키도 코드에 없습니다. 전부 Vercel
  환경변수에만 존재합니다.

---

## 7. 운영 비용

| 항목 | 월 비용 |
| --- | --- |
| Vercel 호스팅 | 무료 구간 |
| Supabase (DB · 인증) | 무료 구간 |
| 도메인 | 연 2만 원 안팎 |

AI 채점을 쓰지 않으므로 사용량에 비례해 오르는 비용이 없습니다.

---

## 8. 아직 확정되지 않은 항목

1. 도메인 `www.frontierexpert.com` — 연결 절차는 아래 「도메인 연결」 참조
2. 문의 양식의 수신 메일 계정 — 확정 전까지 메일 초안으로 넘깁니다
3. 실적 공개 문구 승인 (예: "27건 수행 · 26승 1무")
4. 업무정보실 초기 게재분 — 어떤 자료부터 올릴지

---

## 9. 이전 버전

레벨·포인트·유료 결제·채점이 있던 버전은 지워지지 않았습니다.

```bash
git checkout app1-academy
```

폴더 사본: `../_백업_1번앱_퀴즈아카데미_실제운영본` (`.git` 포함)


## 도메인 연결 — www.frontierexpert.com

코드는 주소를 요청에서 읽으므로, 아래 세 곳만 맞추면 끝납니다. 순서대로 하십시오.

### 1. Vercel — 프로젝트에 도메인 추가

Vercel → 프로젝트 **mnaedu** → **Settings → Domains** → **Add**

| 넣을 도메인 | 설정 |
| --- | --- |
| `www.frontierexpert.com` | 기본 주소 |
| `frontierexpert.com` | 「Redirect to www.frontierexpert.com」 선택 (코드에도 같은 규칙이 있습니다) |

추가하면 Vercel 이 **DNS 에 넣을 값**을 화면에 보여 줍니다. 그 값을 그대로 2번에 쓰십시오.

### 2. 도메인 등록업체 — DNS 레코드

`frontierexpert.com` 을 등록한 곳(가비아·후이즈·카페24 등)의 **DNS 관리**에서:

| 종류 | 호스트 | 값 |
| --- | --- | --- |
| CNAME | `www` | Vercel 화면이 보여 준 값 (예전 값 `cname.vercel-dns.com.` 도 계속 됩니다) |
| A | `@` (또는 비움) | Vercel 화면이 보여 준 IP (2026-09 기준 `216.198.79.1`, 예전 값 `76.76.21.21` 도 계속 됩니다) |

기존에 같은 호스트로 잡힌 레코드가 있으면 지우고 넣으십시오. 반영에 몇 분~수 시간이
걸립니다. SSL 인증서는 Vercel 이 자동으로 발급합니다.

Vercel Domains 화면의 표시가 **Valid Configuration** 이면 끝난 것입니다.
노란 **DNS Change Recommended** 는 오류가 아닙니다 — 예전 값(`cname.vercel-dns.com`,
`76.76.21.21`)으로 잡혀 있어 "새 값으로 바꾸길 권한다"는 뜻일 뿐, 사이트는 그대로
열립니다. 시간이 날 때 화면에 보이는 새 값으로 바꾸시면 표시가 사라집니다.

### 3. Supabase — 회원 로그인 주소 (지금은 필요 없음)

가입 메일 인증을 껐으므로(2026-09-15, Authentication → Providers → Email →
Confirm email 끔) 지금은 Supabase 에 주소를 넣을 일이 없습니다. 가입하면 메일 없이
바로 로그인됩니다. 나중에 **메일 인증을 다시 켜거나 비밀번호 찾기 메일**을 쓰게 되면
그때 아래를 넣으십시오.

Supabase → **Authentication → URL Configuration**

| 항목 | 값 |
| --- | --- |
| Site URL | `https://www.frontierexpert.com` |
| Redirect URLs (추가) | `https://www.frontierexpert.com/auth/callback` |

`mnaedu.vercel.app` 쪽 값은 지우지 말고 함께 두십시오 — 예비 주소로 계속 씁니다.
이 단계를 빼먹으면 메일 속 링크가 옛 주소로 돌아갑니다.

### 확인

- `https://www.frontierexpert.com` 이 열리고 자물쇠(SSL)가 보인다
- `https://frontierexpert.com` 을 치면 `www` 로 넘어간다
- 새 주소에서 회원가입 → 로그인이 된다

https 는 따로 켤 것이 없습니다. DNS 가 맞으면 Vercel 이 인증서를 발급하고, http 로
들어온 요청은 모두 https 로 보내며, 브라우저에 "앞으로 2년은 https 로만 오라"(HSTS)고
알립니다. 다만 **DNS 를 바꾼 직후 몇 분**은 인증서가 아직 없어 브라우저에 「주의 요함」이
뜰 수 있습니다. Vercel Domains 화면의 노란 표시가 사라진 뒤 탭을 닫고 다시 열면
자물쇠가 보입니다. 그래도 남으면 Ctrl+Shift+R 로 새로 고치거나, 윈도우 명령창에서
`ipconfig /flushdns` 를 한 번 치십시오. 크롬은 주소창에서 `www.` 를 감추므로
`frontierexpert.com` 으로만 보여도 정상입니다.

2026-09-15 연결 완료 — 1·2번은 끝났고 위 두 줄은 확인했습니다. 인증서는 두 주소
모두 이날 13시 13분(한국 시각)에 발급되었습니다. 3번은 메일 인증을 껐으므로 넣지
않아도 됩니다. 3번(Supabase 주소)은
회장이 직접 넣으셔야 하고, 넣기 전까지는 새 주소에서 보낸 가입 인증 메일이 옛 주소로
돌아갑니다.

### 그 뒤 (선택)

새 주소가 안정되면 Vercel Domains 에서 `mnaedu.vercel.app` 을 **www 로 보내기**로
바꾸십시오. 검색엔진이 두 주소를 다른 사이트로 세지 않게 하는 마무리입니다.
