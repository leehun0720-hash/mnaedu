# 운영자 안내 — 프론티어 M&A 브랜드 페이지

이 사이트는 **회사 브랜드 페이지**입니다. 실무 문제는 회사가 어느 수준에서
일하는지를 보여주는 장치이고, **정답과 해설은 회원에게만** 열립니다.

교육 과정·레벨·승급·수강료는 없습니다. 회원 가입은 무료이며 결제 기능은
어디에도 없습니다.

---

## 1. 문제 출제 — `/admin`

회장님이 직접 문제를 작성·발행하는 화면입니다.

| 항목 | 설명 |
| --- | --- |
| 업무 영역 | 우호적 M&A · 적대적 M&A/경영권 분쟁 · 경영권 투자 |
| 유형 | 주관식 · 객관식 |
| 문제 | 사이트에 공개됩니다 |
| **정답 · 모범답안** | **회원에게 공개됩니다** |
| **해설** | **회원에게 공개됩니다** — 실무에서 갈리는 지점 |
| 발행 | 체크해야 사이트에 나타납니다 |

붙여넣기 칸에 문제를 통째로 넣으면 업무 영역과 유형을 추정해 아래 항목을
채웁니다. **추정이므로 반드시 확인**하십시오.

발행된 문제가 하나도 없으면 사이트는 기본 문제 6개를 대신 보여주므로,
빈 화면이 나오는 일은 없습니다.

### 옛 앱에서 넘어온 "출제 의도" 필드

1번 앱에는 **비공개를 전제로** 작성하는 `출제 의도` 칸이 있었습니다.
그 내용을 회원에게 보이는 `해설`로 자동 전환하면 작성 당시의 전제가
깨지므로, 데이터는 그대로 두되 **어디에도 공개하지 않습니다**.
공개할 내용이 있다면 회장님이 직접 `해설` 칸에 옮겨 적어 주십시오.

---

## 2. 회원

정답을 열람하려는 사람이 이름·이메일·비밀번호로 가입합니다.
등급도 결제도 없고, 목적은 **누가 보고 있는지 아는 것** 하나입니다.

- 비밀번호는 PBKDF2-SHA256(210,000회)으로 변환해 저장하며 원문은 남지 않습니다.
- 로그인 상태는 서명된 쿠키 30일. 회원 번호와 만료 시각이 서명 안에 들어 있어
  브라우저에서 고칠 수 없습니다.
- 관리자 세션과 회원 세션은 쿠키·시크릿·검증 경로가 모두 분리되어 있어
  서로를 대신할 수 없습니다.

---

## 3. 환경 변수 (Vercel)

저장소가 공개이므로 **어떤 비밀도 코드에 두지 않습니다.**

| 변수 | 용도 |
| --- | --- |
| `ADMIN_PASSWORD` | `/admin` 로그인 비밀번호 |
| `ADMIN_SESSION_SECRET` | 관리자 세션 서명 키 |
| `MEMBER_SESSION_SECRET` | **회원 세션 서명 키 (신규)** |
| `POSTGRES_URL` 등 | Neon 연결 문자열 (자동 주입) |

`MEMBER_SESSION_SECRET`은 2번 앱에서 새로 필요합니다. 다음으로 만들어
Vercel → Settings → Environment Variables 에 넣으십시오.

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
```

이 값이 없으면 회원 가입·로그인이 **503으로 막히고**, 사이트의 나머지 부분은
정상 동작합니다.

---

## 4. 데이터베이스

Vercel 대시보드 → **Storage** → **Create Database** → **Neon**

| 항목 | 값 |
| --- | --- |
| Environments | Production, Preview, **Development** 모두 체크 |
| Custom Prefix | 비워두어도 됩니다 |
| Sensitive | **꺼둡니다** |

Sensitive를 켜면 값을 다시 읽을 수 없어 `vercel env pull`이 빈 값을 가져옵니다.
그때는 Neon 콘솔의 SQL 편집기에서 `drizzle/` 폴더의 SQL을 직접 실행하십시오.

마이그레이션은 `drizzle/0000_puzzling_bastion.sql`(최초 생성),
`drizzle/0001_brand_page_members.sql`(레벨 제거 · 해설 · 회원 테이블) 순으로
적용합니다.

---

## 5. 1번 앱(퀴즈 아카데미)으로 되돌리기

1번 앱은 지워지지 않았습니다.

```bash
git checkout app1-academy
```

폴더 사본은 `../_백업_1번앱_퀴즈아카데미` 에 있습니다(`.git` 포함).
