# 출제 관리자 설정

회장님이 `/admin`에서 직접 문제를 작성·발행하는 기능입니다.
설정 전까지는 사이트가 기존 예시 문제로 그대로 동작하므로, 아래 작업 중에도
운영에는 영향이 없습니다.

## 1. 데이터베이스 만들기

데이터베이스와 회원 시스템은 Supabase를 씁니다. 설정 절차는 `SUPABASE.md`를
따르십시오 — 프로젝트 생성, 마이그레이션 실행, 환경변수까지 그 문서 하나로
끝납니다.

설정 전까지는 사이트가 기존 예시 문제로 그대로 동작하므로, 그 작업 중에도
운영에는 영향이 없습니다.

## 3. 관리자 비밀번호 설정

Vercel 대시보드 → **Settings** → **Environment Variables** 에 두 개를 추가합니다.

| 이름 | 값 |
| --- | --- |
| `ADMIN_PASSWORD` | 회장님이 사용하실 비밀번호 |
| `ADMIN_SESSION_SECRET` | 아무 긴 임의 문자열 (32자 이상 권장) |

`ADMIN_SESSION_SECRET`은 아래로 만들 수 있습니다.

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

추가한 뒤 **재배포**해야 적용됩니다.

> 이 저장소는 공개되어 있습니다. 비밀번호는 절대 코드나 커밋에 넣지 마십시오.
> 위 두 값은 Vercel 설정에만 존재합니다.

## 4. 사용

`https://<도메인>/admin` 에서 비밀번호로 로그인합니다.

- **붙여넣기로 초안 만들기** — 문제를 그대로 붙여넣으면 과정·난이도·유형·보기·정답을
  추정해 아래 입력란을 채웁니다. 추정이므로 저장 전에 확인이 필요합니다.
- **항목별 입력** — 과정, 난이도, 유형을 직접 고르고 문제를 작성합니다.
- **발행** 체크를 켠 문제만 홈페이지 &lsquo;선발 테스트&rsquo;에 최신순으로 표시됩니다.
  체크를 끄면 임시 보관됩니다.

- **해설** — 유료회원이 포인트로 여는 회장 해설입니다. 풀이 화면의 정답 표시
  아래에 놓이고, 열기 전에는 모자이크로 가려집니다.
- **채점함** — 회원이 낸 주관식 답안에 0~100점과 강평을 매깁니다. 60점 이상이면
  통과로 처리되어 회원에게 퀴즈 포인트가 적립되고 통과 레벨이 올라갑니다.
- **회원 관리** — 등급(무료/유료)과 포인트를 조정합니다. 결제 연결 전까지 유료
  전환은 이 화면에서 수동으로 합니다. 버튼 한 번이면 되고 SQL은 필요 없습니다.
  이름·이메일 검색과 등급 필터가 있어 회원이 늘어도 찾을 수 있습니다.
- **출제 현황 (분야 × 레벨)** — 25칸 격자에 분야별·레벨별 문제 수가 뜹니다.
  빗금 친 빈 칸이 아직 출제되지 않은 자리이므로, 목록을 훑는 대신 여기서 다음
  출제를 정하십시오. 칸을 누르면 그 칸의 문제만 아래 목록에 걸립니다.

### 문제가 많아지면

문제 목록은 20건씩 끊어서 보여 주고, 아래 도구로 좁힙니다.

- **검색** — 문제 본문에 들어간 말로 찾습니다.
- **분야 · 레벨 · 상태 필터** — 상태의 `미완성`은 정답이나 해설이 비어 있는
  문제만 골라냅니다. 발행 전에 손볼 것이 남은 문제를 한 번에 모읍니다.
- **목록에서 바로 발행/발행 취소** — 편집 화면을 열지 않고 켜고 끕니다.
- 목록의 `정답 없음` · `해설 없음` 배지로 미완성 문제를 바로 알아봅니다.

채점함도 같은 방식입니다. 기본은 **채점 대기**만 보여 주므로 회원이 늘어도
할 일이 지난 채점에 묻히지 않고, 대기 건수는 제목에 항상 떠 있습니다.

**정답과 출제 의도는 홈페이지에 전달되지 않습니다.** 공개 페이지로 나가는 데이터에서
아예 빠지므로, 브라우저 개발자 도구로도 볼 수 없습니다.

## 동작하지 않을 때

| 화면 | 원인 |
| --- | --- |
| "관리자 설정이 필요합니다" | `ADMIN_PASSWORD` 또는 `ADMIN_SESSION_SECRET` 없음 |
| "데이터베이스가 연결되지 않았습니다" | `POSTGRES_URL` 없음 |
| 로그인은 되는데 저장 실패 | 2단계 테이블 생성을 하지 않음 |
