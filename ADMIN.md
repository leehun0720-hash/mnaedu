# 관리자 설정

`/admin`은 회장님이 문제와 자료를 직접 올리는 **관리자** 화면입니다. 하는 일은
넷입니다 — 문제 출제, 칼럼, 업무정보실, 회원 관리.

첫 화면 맨 아래의 **「관리자 ↗」**를 누르면 새 창으로 열립니다. 출제 도중
홈페이지를 확인하실 때 쓰시던 화면이 사라지지 않게 하려는 것입니다.

설정 전까지도 홈페이지는 예시 문제로 그대로 동작하므로, 아래 작업 중에 운영이
멎지 않습니다.

## 1. 데이터베이스

데이터베이스와 회원 로그인은 Supabase를 씁니다. 절차는 `SUPABASE.md` 하나로
끝납니다 — 프로젝트 생성, 마이그레이션, 환경변수까지.

## 2. 관리자 비밀번호

Vercel 대시보드 → **Settings** → **Environment Variables** 에 두 개를 추가합니다.

| 이름 | 값 |
| --- | --- |
| `ADMIN_PASSWORD` | **처음** 들어가실 비밀번호 (한 번 바꾸신 뒤에는 화면에서 정한 값이 기준입니다) |
| `ADMIN_SESSION_SECRET` | 아무 긴 임의 문자열 (32자 이상 권장) |

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

추가한 뒤 **재배포**해야 적용됩니다.

> 이 저장소는 공개되어 있습니다. 비밀번호는 절대 코드나 커밋에 넣지 마십시오.
> 위 두 값은 Vercel 설정에만 존재합니다.

## 3. 문제 출제

- **붙여넣기로 초안 만들기** — 문제를 통째로 붙여넣으면 분야·유형·보기·정답을
  추정해 입력란을 채웁니다. 추정이므로 저장 전에 확인이 필요합니다.
- **문제 본문은 공개**됩니다. **정답과 해설은 로그인한 회원에게만** 열립니다.
- **발행** 체크를 켠 문제만 홈페이지 &lsquo;실무 문제&rsquo;에 최신순으로 올라갑니다.
- **출제 현황** — 5개 분야별로 몇 건이 발행/작성되었는지 보여 줍니다. 빗금 친
  칸이 아직 비어 있는 분야이므로, 다음 출제를 여기서 정하십시오. 칸을 누르면
  그 분야만 목록에 걸립니다.
- 목록의 `정답 없음` · `해설 없음` 배지로 손이 더 가야 할 문제를 골라냅니다.

### 채용시험 문제도 여기서 냅니다

분야 칸의 맨 아래 **「직원채용 (채용시험)」** 을 고르시면 채용시험 문제가 됩니다.
쓰는 방법은 업무 문제와 똑같습니다 — 문제 · 정답 · 해설을 적고 발행을 켜면 됩니다.

다만 서는 자리가 다릅니다.

| | 업무 문제 | 채용시험 문제 |
| --- | --- | --- |
| 서는 곳 | 그 분야 업무 화면 · 첫 화면 새 소식 | `/careers` 응시 화면 **한 곳뿐** |
| 정답을 보는 사람 | 로그인한 회원 | **아무도 못 봅니다** — 회장만, 관리자 화면에서 |
| 쓰임 | 회사의 수준을 보이는 자료 | 지원자를 가리는 시험지 |

채용 문제의 정답은 회원이 정답을 여는 길에서도 막힙니다. 시험지에 답이 인쇄되어
나가는 일이 없도록 두 겹으로 막아 두었습니다.

### 옛 "출제 의도" 필드

이전 앱에는 **비공개를 전제로** 쓰는 `출제 의도` 칸이 있었습니다. 그 내용을
회원에게 보이는 `해설`로 자동 전환하면 작성 당시의 전제가 깨지므로, 데이터는
그대로 두되 **어디에도 공개하지 않습니다.** 관리자 목록에서 참고용으로만 보이며,
공개할 내용이라면 회장님이 직접 `해설` 칸에 옮겨 적어 주십시오.

## 3-2. 서식 편집기 — 칼럼과 자료의 본문

칼럼과 자료의 본문 칸은 **구글 메일 글쓰기 창과 같은 편집기**입니다.
굵게 · 기울임 · 밑줄 · 취소선 · 소제목 · 인용 · 글머리/번호 목록 · 링크 · 되돌리기.

워드에서 그대로 붙여넣으셔도 됩니다 — 서식은 살고, 워드가 딸려 보내는
글꼴·여백 같은 잡동사니는 빠집니다. 링크는 `http://` 또는 `https://` 로
시작하는 주소만 걸립니다. 그림은 넣지 않습니다.

이미 올려 둔 글(서식 없는 평문)은 그대로 문단으로 보이고, 수정하시면 편집기에
그대로 열립니다.

## 4. 칼럼 — 글을 올리는 곳

**글은 여기서 올리십시오.** 제목과 본문만 붙여넣으면 됩니다.
아주경제 연재분을 옮기실 때도 같은 자리를 씁니다.

업무정보실과 다른 점이 하나 있고, 그게 이 메뉴를 따로 둔 이유입니다 —
**칼럼은 웹 페이지로 발행되어 검색에 잡힙니다.** 워드 파일로 올리면 검색엔진이
본문을 읽지 못해, 연재 100여 회를 얹어도 검색 자산이 되지 않습니다.

### 분야를 정하면 그 분야 업무정보실에도 섭니다

**분야**를 고르시면 그 분야 화면의 **업무자료**에 파일 자료와 나란히 서고,
누르면 글을 읽는 쪽으로 이어집니다. 「내려받기」 대신 「읽기」로 표시됩니다.

한 번 쓰신 글이 두 곳(칼럼 목록 · 그 분야 업무정보실)에 섭니다. 분야를 비우시면
칼럼 목록에만 섭니다.

| 항목 | 설명 |
| --- | --- |
| 제목 | 그대로 글 제목이자 주소가 됩니다 |
| 본문 | 워드에서 복사해 붙여넣으십시오. **빈 줄이 문단을 나눕니다** |
| 게재처 · 게재일 | 예: 아주경제 / 원문이 실린 날. 이 날짜순으로 정렬됩니다 |
| 분야 | 5분야 중 하나 (선택) |

**발행** 체크를 켠 글만 `/insights`에 올라갑니다. 목록의 `보기`를 누르면 실제
페이지가 열립니다.

- 주소는 제목에서 한 번 만들어지고 **이후 제목을 고쳐도 바뀌지 않습니다** —
  주소가 바뀌면 이미 걸린 링크와 검색 색인이 끊기기 때문입니다.
- 서식(굵게·표 등)은 지원하지 않습니다. 붙여넣기 한 번으로 끝내는 것이
  목적이라 문단만 나눕니다.
- 삭제하면 그 주소도 함께 사라집니다.

## 5. 업무정보실 — 분야별 업무자료

**칼럼처럼 제목과 본문을 붙여넣습니다.** 파일을 첨부하지 않습니다 (회장 지시
2026-09-15). 올린 자료는 고르신 **분야 화면의 「업무자료」**에 서고, 누르면 그
자리에서 읽힙니다.

| 항목 | 값 |
| --- | --- |
| 제목 · 본문 | 워드에서 그대로 붙여넣으십시오. 빈 줄이 문단을 나눕니다. 그 밖의 설명 칸은 없습니다 |
| 분야 | **필수** — 이 분야 화면에 올라갑니다 |
| 구분 | 자료 / 칼럼 (표시 이름일 뿐입니다) |

칼럼 탭과 다른 점: 칼럼은 검색용 웹 글(`/insights`)에도 서고, 업무정보실 글은 그
분야 화면에만 섭니다. 검색에 잡히길 원하시면 칼럼으로 올리십시오 — 분야를
정하시면 칼럼도 그 분야 업무정보실에 함께 섭니다.

예전에 파일로 올린 자료는 그대로 내려받힙니다. 제목·분야·발행 여부는 고칠 수
있지만 본문은 없습니다.

**발행** 체크를 켠 자료만 홈페이지에 나타납니다.

### 이미 올린 자료를 고치려면

목록의 **수정**을 누르면 위 양식이 그 자료로 채워집니다 — 본문까지 그대로
불러옵니다. 고치고 **수정 저장**을 누르십시오.

### 어느 분야에나 올라갑니다

패밀리오피스와 투자가 클럽에도 자료를 올리실 수 있습니다. 그 두 분야에서
웹에 두지 않는 것은 **평가문제**이지 자료가 아닙니다.

## 5-2. 두 오피스 모두에서 운용합니다

M&A 오피스(중개 · 경영권 분쟁 · 자금조달)와 시크릿 오피스(패밀리오피스 ·
투자가 클럽) **다섯 분야 모두** 문제를 출제하고 자료를 운용하실 수 있습니다.

전에는 시크릿 오피스 두 분야를 분야째 가려 두었습니다. 지금은 **발행 체크
하나가 그 자리를 맡습니다** — 어느 분야든 발행하지 않으신 것은 목록에도,
직접 링크로도, 정답·해설 경로에도 오르지 않습니다. 무엇을 세울지는 이제
그때그때 회장님이 정하십니다.

두 분야 화면에는 「웹 안내 범위」 고지가 그대로 서서, 모임의 세부와 가입
절차는 오프라인에서 안내한다는 점을 밝힙니다.

바뀌지 않은 것: **정답과 해설은 어느 분야에서도 공개 화면에 실리지 않습니다.**
로그인한 회원에게만 열립니다.

## 6. 회원 관리

정답과 해설을 보려고 등록한 분들의 명단입니다. 등급도 포인트도 결제도 없으므로,
여기서 하시는 일은 넷뿐입니다.

| 하시는 일 | 설명 |
| --- | --- |
| **찾기** | 성함 · 이메일 · 메모를 한 번에 훑습니다 |
| **이름 바로잡기** | 이름을 안 적고 가입하신 분을 채워 넣을 수 있습니다 |
| **메모** | 회장님만 보십니다. 회원 화면에는 나오지 않습니다 |
| **명단에서 내리기** | 우리 쪽 명단에서만 내립니다 (아래 주의) |

메모는 오프라인 심사를 거쳐 모임에 들이는 분야(패밀리오피스 · 투자가 클럽)에서
누가 어느 자리까지 왔는지 적어 두시라고 둔 칸입니다. 등급이 아니라 메모입니다 —
코드가 이 값을 읽어 무엇을 판정하지 않습니다.

> **단추 이름이 둘입니다 — 지금 무엇이 지워지는지 그대로 말합니다.**
>
> | 단추 | 뜻 |
> | --- | --- |
> | **「회원 삭제」** | 명단과 **계정까지** 지웁니다. 되돌릴 수 없습니다 |
> | **「명단에서 내리기」** | 명단에서만 내립니다. 계정이 남아 **로그인이 계속 됩니다** |
>
> 배포 설정에 Supabase 관리 키(`SUPABASE_SECRET_KEY`)가 있으면 앞쪽, 없으면
> 뒤쪽입니다. 넣는 방법은 `SUPABASE.md`에 있습니다.

### 명단에 오르는 조건

**메일로 보낸 인증 링크를 눌러 가입을 마치신 분**만 오릅니다. 신청만 하고 인증을
마치지 않으신 분은 여기에 나타나지 않습니다 — 그런 분까지 보시려면 Supabase의
**Authentication → Users** 에서 `Last sign in` 이 비어 있는 계정을 보십시오.

이메일은 신원이라 이 화면에서 바꾸지 않습니다. 바꿔야 한다면 그분이 새로
가입하셔야 합니다.

## 7. 지원자 — 직원채용

첫 화면의 **직원채용** 칸이나 `/careers` 로 들어온 분이 채용시험 문제를 풀고
「직원채용」을 골라 보내면 이 탭에 한 건씩 쌓입니다.

### 들어온 지원서를 보는 법

- 위쪽 **접수 · 검토중 · 합격 · 불합격** 단추는 그 상태의 건수입니다. 누르면
  그 상태만 걸러 봅니다. 다시 누르면 전체로 돌아옵니다.
- 이름을 누르면 지원서가 아래에 펼쳐집니다. 성함 · 지원 구분 · 이메일 · 연락처와
  지원 동기, 그리고 답안이 문항별로 섭니다.
- 답안 오른쪽에는 **회장님이 등록해 두신 정답**이 나란히 놓입니다. 두 칸을 견주며
  읽으시면 됩니다.

### 채점

- **점수**는 0~100으로 적습니다. **비워 두면 「채점 전」** 입니다 — 0점과 다릅니다.
- **전형 상태**를 옮기고 **메모**를 남기신 뒤 「채점 저장」을 누르십시오.
- 메모는 회장님만 보십니다. 지원자에게는 점수도 상태도 메모도 어느 경로로도
  나가지 않습니다. **합격 여부는 회장님이 따로 알리셔야 합니다.**

### 응시자 쪽에서는 이렇게 보입니다

문제를 풀고 → 지원 구분을 고르고 → 성함·이메일을 적고 → 개인정보 동의에 표시하면
접수번호가 뜹니다. 채점 결과는 그 화면에 나오지 않습니다.

같은 이메일로는 **하루에 한 번만** 접수됩니다. 손이 미끄러져 두 번 눌리는 일과
장난으로 밀어 넣는 일을 함께 막기 위해서입니다.

### 지우실 때

목록의 **삭제**를 누르면 답안과 연락처가 함께 사라지며 되돌릴 수 없습니다.
전형이 끝난 지원서는 개인정보처리방침대로 파기하셔야 하므로, 그때 쓰는 단추입니다.

## 8. Q&A — 묻고 답하기

홈페이지의 [Q&A 화면](https://www.frontierexpert.com/qna)에서 누구나 질문을 남길 수
있습니다. 그 질문이 이 탭에 쌓입니다.

### 들어온 질문을 보는 법

- 위쪽 **답변대기 · 미게시 · 비밀글** 단추로 걸러 봅니다. 다시 누르면 전체로 돌아옵니다.
- 질문 아래 **「답변 쓰기」** 를 누르면 답을 적는 칸이 열립니다.

### 답을 달고 세우는 법

| 단추 | 하는 일 |
| --- | --- |
| 저장하고 게시 | 답을 저장하고 질문·답을 함께 게시판에 세웁니다. 보통 이것을 쓰십니다. |
| 저장만 | 답만 저장합니다. 게시판에는 아직 서지 않습니다. |
| 게시판에 세우기 / 내리기 | 답과 상관없이 세우거나 내립니다. |
| 비밀글로 / 비밀글 풀기 | 게시판에서 감추거나 되돌립니다. |
| 삭제 | 질문과 답이 함께 사라집니다. 되돌릴 수 없습니다. |

### 꼭 아셔야 할 것 셋

1. **보낸 질문은 곧바로 게시판에 서지 않습니다.** 회장님이 세우셔야 섭니다. 공개
   게시판을 그냥 열어 두면 광고와 비방이 먼저 자리를 잡기 때문입니다.
2. **비밀글은 게시판에 아예 오르지 않습니다.** 그래서 비밀글에는 「게시」 단추가
   나오지 않습니다. 답은 적어 주신 이메일로 **회장님이 직접** 보내셔야 합니다.
3. **질문자의 이메일은 어떤 공개 화면에도 나가지 않습니다.** 회신용으로만 받습니다.

### 자주 묻는 질문은 따로입니다

Q&A 화면 위쪽의 「자주 묻는 질문」 여섯 개는 코드에 적힌 고정 문구입니다. 그것을
고치시려면 알려 주십시오. 게시판은 그 아래입니다 — 같은 질문이 게시판에 반복해서
쌓이지 않도록 먼저 읽게 하는 자리입니다.

## 동작하지 않을 때

| 화면 | 원인 |
| --- | --- |
| "관리자 설정이 필요합니다" | `ADMIN_PASSWORD` 또는 `ADMIN_SESSION_SECRET` 없음 |
| "데이터베이스가 연결되지 않았습니다" | `POSTGRES_URL` 없음 (SUPABASE.md 참조) |
| 문제만 저장이 안 됨 (칼럼·자료는 됨) | 옛 questions 표에 폐지된 `level` 열이 NOT NULL 로 남아 있습니다. `supabase/setup.sql` 을 다시 한 번 실행하면 정리됩니다 |
| 로그인은 되는데 저장 실패 | 마이그레이션 미실행 — 화면에 뜬 문장이 setup.sql을 가리키면 SUPABASE.md의 SQL을 한 번 실행하십시오 |
| "데이터베이스가 응답하지 않아…" | Supabase가 잠시 멎었거나 프로젝트가 일시 정지됨 |
| 자료 올리기가 413으로 거부됨 | 파일이 8 MB를 넘었습니다 |

## 이전 앱으로 되돌리기

레벨·포인트·유료 결제가 있던 버전은 지워지지 않았습니다.

```bash
git checkout app1-academy
```

폴더 사본은 `../_백업_1번앱_퀴즈아카데미_실제운영본` 에 있습니다(`.git` 포함).


---

## 비밀번호 바꾸기

`/admin` → **비밀번호** 탭에서 직접 바꾸실 수 있습니다. 배포 설정에 들어가실
필요가 없습니다.

1. 지금 쓰시는 비밀번호를 넣습니다 (자리를 비운 사이 열린 화면을 누가 만졌을
   수 있으므로 한 번 더 확인합니다)
2. 새 비밀번호를 10자 이상으로 넣고, 확인란에 한 번 더 넣습니다
3. **비밀번호 바꾸기**

바꾸시면 **다른 기기·브라우저에 남아 있던 로그인이 모두 끊깁니다.** 바꾸신
그 화면만 그대로 이어집니다.

### 어떻게 담기는가

- 비밀번호 원문은 어디에도 남지 않습니다. 되돌릴 수 없는 형태(PBKDF2,
  반복 210,000회)로 바꿔 `admin_credentials` 표에 담습니다.
- 그 표는 RLS와 권한 회수로 공개 키에서 완전히 차단됩니다
  (`supabase/setup.sql`이 설정합니다).
- 한 번도 바꾸신 적이 없으면 환경변수 `ADMIN_PASSWORD`가 기준입니다.
  한 번 바꾸신 뒤에는 저장된 값만 통하며, 환경변수는 무시됩니다.

### 잊으셨을 때

TEN AI에 연락 주십시오. 데이터베이스에서 저장된 값을 지우면 환경변수
`ADMIN_PASSWORD`가 다시 기준이 됩니다.

```sql
DELETE FROM admin_credentials;
```
